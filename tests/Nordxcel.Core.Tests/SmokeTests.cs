namespace Nordxcel.Core.Tests;

/// <summary>
/// Teste de fumaça: existe apenas para provar que o runner de testes está
/// configurado e enxerga o projeto Core. Pode ser removido assim que o
/// primeiro teste de verdade (Fase 4 do roadmap) entrar no lugar.
/// </summary>
public class SmokeTests
{
    [Fact]
    public void TestRunner_Funciona()
    {
        Assert.True(true);
    }
}
